#include <stdio.h>
#include <stdbool.h>

int main() {
    bool flag = true;
    
    if (flag) {
        printf("Флаг истинен\n");
    } else {
        printf("Флаг ложен\n");
    }
    return 0;
}