#include <stdio.h>

// 1. Синтаксическая ошибка - не скомпилируется
// int main() {
//     printf("Hello"  // пропущена закрывающая скобка

// 2. Семантическая ошибка - синтаксис верен, но смысл неверен
int x = 5;
int y = 2;
int z = x / y;  // Результат 2, а не 2.5 (целочисленное деление)

// 3. Логическая ошибка - программа работает, но неверно
int sum = 0;
for (int i = 1; i <= 10; i++) {
    sum += i;  // Должно быть sum += i, но написали sum = i
}

// 4. Runtime ошибка - падение во время выполнения
// int *ptr = NULL;
// *ptr = 10;  // Segmentation fault

int main() {
    printf("Демонстрация типов ошибок\n");
    return 0;
}